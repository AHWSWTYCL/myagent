export const toolName = 'CreateTeam'
export const toolDescription = '创建一个命名的 team（协作组"房间"）。Team 是 leader + N 个 teammate worker 的文件式分组，存储在 ~/.myagent/teams/<name>/team.json。创建 team 后，可用 agent(agent="teammate", team_name=...) 或 start_teammate 将 worker 加入 team。'
