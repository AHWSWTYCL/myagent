export const toolName = 'Memory'
export const toolDescription = '管理自动记忆系统。支持 save（记录）、delete（删除）、list（查看）、update_index（刷新索引）四种操作。分类存储：profile（用户画像）、project（项目决策）、feedback（用户反馈）、reference（外部引用）。只存"读代码读不出来"的信息。'
